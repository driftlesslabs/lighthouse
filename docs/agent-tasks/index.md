# Task guide index

Read only a guide whose trigger matches the current task. Paths are relative to this index.

- [Add a constraint component](add-constraint-component.md) — Use when
  adding a person/household constraint attribute or substantially changing its meaning. Starts with
  user clarification and a confirmed component contract. Excludes routine calibration under an
  agreed specification, unrelated components, and general cleanup. Native discovery is available
  through `.agents/skills/add-constraint-component/SKILL.md` at the repository root.

When adding a guide, list its task name, precise trigger, exclusions, and relative link here. Use
`TEMPLATE.md` only when authoring a guide, not when performing ordinary repository work.
