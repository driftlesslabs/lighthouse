# <Task name>

Authoring template only. Replace placeholders and remove authoring notes before registering a guide.

## When to use

Specify the user intent that activates this guide and examples of tasks it excludes. Prefer intent
over broad keywords or directory matches.

## Required context

List required inputs, decisions, and targeted references. State which missing information requires
clarification and which choices have defaults. Link large references instead of embedding them.

## Workflow

Give concrete steps in dependency order. Link examples and supporting documents at the step that
needs them. Separate mandatory requirements from recommendations. Keep unrelated workflows out.

## Validation and completion

Specify observable acceptance criteria, relevant tests, reproducibility requirements, and expected
deliverables. Give commands with prerequisites and working directories when known. Define what to
report if validation cannot run.
